# Shawn — Paper Starter

Use this with the shared `PAPER-TEMPLATE.md`.

Your curation work is already real research material. The draft should make that visible.

## Main question

When the same prompts are run through general-purpose and style-tuned image models, how much of “style control” is real and how much is just surface-level style vocabulary?

## Backup question

Does fine-tuning for one style improve style fidelity at the cost of flexibility outside that style?

## Best artifact to anchor the paper

Use **AnimeSceneWriter** if possible, or the comparison idea that sits behind it if the Space is still unstable.

## What to paste into the master prompt

- the exact models you want to compare
- the exact styles or prompt set
- one example where a model looked impressive but did not really follow the style deeply
- one example where a specialized model did better inside its lane
- one honest limitation such as runtime issues, missing journal weeks, or still-small test grid

## What kind of draft you should expect

You should be able to get a real rough `PAPER.md` tonight.

It should:

- describe the comparison clearly
- use one actual prompt/style example
- make one claim about style specificity or specialization
- include one honest limitation

## If the AI gets too broad

Push it back toward:

- one comparison
- one prompt grid
- one specialization tradeoff
